seed: 31

wandb_project_name: null  # If specified wandb is used

adv_data_path: null  # Path to adversarial data
clean_data_path: null  # Path to clean dataset

batch_size: 1  # Batch size for training
num_samples: 100  # Number of samples to process
input_res: 224  # Input image resolution
alpha: 1.0  # Some scaling factor
epsilon: 1.0  # Epsilon parameter

sigma: 16.0  # Some floating-point parameter
